# MACHINE — 이 머신 전용 메모

이 머신에만 해당하는 절대 경로·OS 전용 명령을 적는 곳이다.
`docs/MACHINE.md`로 복사해서 쓰면 되고, 그 사본은 git에 올라가지 않는다(지식 파일 오염 방지).

## 자주 쓰는 절대 경로

- (예: 다른 저장소·데이터 폴더 등 매번 다시 입력하게 되는 경로를 적는다)

## 이 머신 전용 명령

- (예: 이 OS·셸에서만 통하는 실행/빌드 명령을 적는다)
- Playwright(`webapp-testing` 스킬 전제조건) 설치: `pip install playwright && playwright install` — 파이썬/가상환경 경로가 머신마다 달라 여기에 실제로 통하는 형태를 적어둔다
